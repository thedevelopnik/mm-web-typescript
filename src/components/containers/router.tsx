import * as React from 'react';
import { BrowserRouter } from 'react-router-dom';
import { App } from './App';

export default (
    <BrowserRouter>
        <div>
            <App />
        </div>
    </BrowserRouter>
);
